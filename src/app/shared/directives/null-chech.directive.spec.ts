import { NullChechDirective } from './null-chech.directive';

describe('NullChechDirective', () => {
  it('should create an instance', () => {
    const directive = new NullChechDirective();
    expect(directive).toBeTruthy();
  });
});
